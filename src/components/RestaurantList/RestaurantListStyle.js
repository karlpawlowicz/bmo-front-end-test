import styled from 'styled-components';

import { flex, display } from '../../GlobalStyle';

export const List = styled.ul`
  ${flex};
  ${display};
  margin-left: -15px;
  margin-right: -15px;
  padding: 0;
`;
